# All Duplicate Subtrees

## Problem Statement

Given the root of a binary tree, find all duplicate subtrees present in the tree.

Two subtrees are considered duplicates if they have the same structure and identical node values at corresponding positions.

Return the root of each tree in the form of a list array and the driver code will print the tree in pre-order tree traversal in lexicographically increasing order.

## Problem Link
https://www.geeksforgeeks.org/problems/duplicate-subtrees/1
